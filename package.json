{
  "name": "daifaxitong",
  "version": "0.1.0",
  "private": true,
  "description": "一件代发系统第一版：商品、供应商、订单、发货、Excel 导入导出。",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm start -w server",
    "db:init": "npm run db:init -w server",
    "deploy:check": "npm test -w server && npm run build",
    "test": "npm test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
